---
phase: 04-exam-submit-export
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/server/src/lib/exam/score-question.ts
  - apps/server/src/lib/exam/score-question.test.ts
  - apps/server/src/lib/exam/submit.ts
  - apps/server/src/lib/exam/transition.ts
  - apps/server/src/routes/api/admin/exams-lifecycle.ts
  - apps/server/src/routes/api/admin/exams-submissions.ts
  - apps/server/src/routes/api/student/exam-submit.ts
  - apps/server/src/routes/api/student/exam-submission.ts
  - apps/server/src/routes/api/student/index.ts
  - apps/server/src/index.ts
  - apps/web/src/lib/exam.ts
  - apps/web/src/lib/student.ts
  - apps/web/src/pages/AdminExams.tsx
  - apps/web/src/pages/AdminExamDetail.tsx
  - apps/web/src/pages/AdminDashboard.tsx
  - apps/web/src/pages/StudentExamTake.tsx
  - apps/web/src/router.tsx
  - apps/web/src/components/auth/StudentRoute.tsx
  - apps/web/src/components/ui/radio-group.tsx
  - apps/web/src/components/ui/checkbox.tsx
  - apps/web/src/components/ui/alert-dialog.tsx
autonomous: true
requirements:
  - EXAM-01
  - EXAM-02
must_haves:
  truths:
    - "Student can submit once while exam IN_PROGRESS; server computes totalScore using QBANK-02 rules"
    - "Duplicate POST submit returns 409 with Chinese message per EXAM-02"
    - "After submit, PUT answers rejected; student can GET read-only submission replay"
    - "Teacher can explicitly end exam (IN_PROGRESS to ENDED) per D-04; students cannot write after ENDED"
    - "Admin detail shows submission list with masked national ID, score, submitted flag"
  artifacts:
    - path: apps/server/src/lib/exam/score-question.ts
      provides: scoreQuestion aligned with normalize-answer.ts
      exports: scoreQuestion
    - path: apps/server/src/lib/exam/submit.ts
      provides: submitExam transaction
    - path: apps/server/src/routes/api/student/exam-submit.ts
      provides: POST submit
  key_links:
    - from: apps/server/src/routes/api/student/exam-submit.ts
      to: lib/exam/submit.ts
      via: prisma.$transaction
      pattern: submission\.create
    - from: apps/server/src/lib/exam/score-question.ts
      to: apps/server/src/lib/qbank/normalize-answer.ts
      via: normalizeAnswerKeys + splitAnswerTokens
      pattern: normalizeAnswerKeys
---

## Phase Goal

**As a** 名单内考生, **I want to** 一次性提交试卷并在提交后无法篡改答案, **so that** 成绩可信且教师可收卷（EXAM-01 完成 + EXAM-02 + **D-04**）。

<objective>
交付 **提交与收卷垂直切片**：服务端 **提交时计分**（对齐 `02-ACCEPTANCE.md` QBANK-02）、`Submission` 唯一约束与 **409 拒重复提交**（EXAM-02）、教师 **结束考试**（**D-04**）、管理端成绩列表与学生只读回放。
</objective>

<execution_context>
@E:/programs/LAN_exam/.cursor/get-shit-done/workflows/execute-plan.md
@E:/programs/LAN_exam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-exam-submit-export/04-RESEARCH.md
@.planning/phases/02-qbank-import/02-ACCEPTANCE.md
@.planning/phases/04-exam-submit-export/04-01-SUMMARY.md
@apps/server/src/lib/qbank/normalize-answer.ts
@apps/server/src/lib/exam/access.ts
@apps/server/src/lib/exam/transition.ts

<interfaces>
From apps/server/src/lib/qbank/normalize-answer.ts:
- normalizeAnswerKeys(type, raw, optionKeys): string | null
- splitAnswerTokens(raw): string[]

QBANK-02 (02-ACCEPTANCE.md):
- MULTI + ALL_OR_NOTHING: selected set must equal answerKeys exactly → points else 0
- SINGLE/JUDGE: normalized string equality → points else 0

submitExam contract:
- Input: examId, rosterEntryId from session
- Transaction: if Submission exists → throw CONFLICT; load drafts + questions; score each; create Submission + Answer rows; deleteMany AnswerDraft
- Response: { ok: true, totalScore, submittedAt }

POST /api/student/exam/submit body { examId }
GET /api/student/exam/submission?examId= read-only own answers: per item { examQuestionId, selectedKeys, isCorrect, pointsAwarded } — **no answerKeys** (RESOLVED Q2 in 04-RESEARCH.md)
POST /api/admin/exams/:id/end → ENDED (D-04)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scoreQuestion 单元测试与实现（QBANK-02）</name>
  <read_first>
    - .planning/phases/02-qbank-import/02-ACCEPTANCE.md
    - apps/server/src/lib/qbank/normalize-answer.ts
    - apps/server/src/lib/roster/national-id.test.ts
    - .planning/phases/04-exam-submit-export/04-RESEARCH.md
  </read_first>
  <files>apps/server/src/lib/exam/score-question.ts;apps/server/src/lib/exam/score-question.test.ts</files>
  <behavior>
    - MULTI ALL_OR_NOTHING: selected A,B,C matching answerKeys A,B,C → pointsAwarded = points
    - MULTI: missing B (A,C only) → 0
    - MULTI: extra wrong option → 0
    - SINGLE: selected A when answer A → full points
    - JUDGE: 正确 vs 错误 normalized match
    - Empty/invalid selection → 0 points, isCorrect false
  </behavior>
  <action>
    **RED→GREEN：** 新建 `score-question.test.ts`（`node:test` + tsx）覆盖上表用例；再实现 `scoreQuestion(q, optionKeys, selectedRaw)` 返回 `{ selectedKeys, isCorrect, pointsAwarded }`。`MULTI` 比较必须 `splitAnswerTokens(sel).join(',') === splitAnswerTokens(q.answerKeys).join(',')` 且 `multiScoringRule === 'ALL_OR_NOTHING'`。`SINGLE`/`JUDGE` 用 `normalizeAnswerKeys` 后字符串相等。**禁止** 前端计分或重复实现 split 规则。
  </action>
  <verify>
    <automated>node --import tsx --test apps/server/src/lib/exam/score-question.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `score-question.test.ts` 全部通过
    - 测试文件至少 6 个 `it(` 用例
    - `score-question.ts` 从 `normalize-answer.js` 导入 `normalizeAnswerKeys`, `splitAnswerTokens`
  </acceptance_criteria>
  <done>计分函数与 QBANK-02 验收表一致。</done>
</task>

<task type="auto">
  <name>Task 2: 提交事务、结束考试、学生/教师 API</name>
  <read_first>
    - apps/server/src/lib/exam/access.ts
    - apps/server/src/lib/qbank/import-questions.ts
    - apps/server/src/routes/api/student/exam-answers.ts
    - .planning/phases/04-exam-submit-export/04-CONTEXT.md
  </read_first>
  <files>apps/server/src/lib/exam/submit.ts;apps/server/src/lib/exam/transition.ts;apps/server/src/routes/api/admin/exams-lifecycle.ts;apps/server/src/routes/api/admin/exams-submissions.ts;apps/server/src/routes/api/student/exam-submit.ts;apps/server/src/routes/api/student/exam-submission.ts;apps/server/src/routes/api/student/index.ts;apps/server/src/index.ts</files>
  <action>
    **`submit.ts`：** `submitExam(prisma, { examId, rosterEntryId })` — `assertStudentExamAccess(..., 'submit')`；若已有 `Submission` → 409 domain error「您已提交过本场考试，无法再次提交。」；`$transaction`：加载全部 `ExamQuestion`+`Question`+`options`；合并 `AnswerDraft`（无 draft 的题按空串计分）；每题 `scoreQuestion`；`submission.create` + `answer.createMany`；`answerDraft.deleteMany`；返回 `totalScore`, `submittedAt`。

    **`transition.ts` 补全 `endExam`（D-04）：** 仅 `IN_PROGRESS` → `ENDED`，`endedAt=now()`；`ENDED` 后 `exam-answers`/`submit` 已因 access 拒绝。

    **`exams-lifecycle.ts`：** 增加 `POST /api/admin/exams/:id/end` + AlertDialog 文案由前端处理。

    **`exams-submissions.ts`：** `GET /api/admin/exams/:id/submissions` — 左连接名单批次全部 `RosterEntry`，左连接 `Submission`；返回 `{ items: [{ rosterEntryId, fullName, nationalId, totalScore|null, submitted: boolean, submittedAt|null }] }`（证号全量供前端 `maskNationalId`）。`GET .../submissions/:rosterEntryId` 教师只读单生 `Answer` 明细（含对错、得分、所选；可含 `answerKeys` 供教师核对）。

    **Student routes：** `exam-submit.ts` `POST` body `{ examId }` + rateLimit 10/min；`exam-submission.ts` `GET` 已提交回放 — 响应每题仅 `selectedKeys`, `isCorrect`, `pointsAwarded`（**禁止** `answerKeys`）。注册 `index.ts`。

    **EXAM-02：** 已提交后 `PUT answers` 返回 409；`POST submit` 重复 409。
  </action>
  <verify>
    <automated>pnpm --filter @lan-exam/server exec tsc -b --noEmit</automated>
    <automated>node --import tsx --test apps/server/src/lib/exam/score-question.test.ts</automated>
    <automated>rg -q "submitExam\|endExam" apps/server/src/lib/exam</automated>
  </verify>
  <acceptance_criteria>
    - 首次 `POST /api/student/exam/submit` 返回 200 且 DB 有唯一 `Submission`
    - 第二次同 exam 同学生 submit 返回 409 且 `totalScore` 不变
    - `POST .../end` 后 `PUT answers` 返回 409
    - `GET /api/admin/exams/:id/submissions` 含未提交考生 `submitted: false`
  </acceptance_criteria>
  <done>提交、收卷、成绩列表 API 就绪。</done>
</task>

<task type="auto">
  <name>Task 3: 管理端考试 UI + 学生答题页（EXAM-01 前端）</name>
  <read_first>
    - .planning/phases/04-exam-submit-export/04-UI-SPEC.md
    - .planning/phases/04-exam-submit-export/04-ACCEPTANCE.md
    - .planning/phases/04-exam-submit-export/04-PATTERNS.md
    - apps/web/src/pages/AdminRoster.tsx
    - apps/web/src/pages/StudentLogin.tsx
    - apps/web/src/components/admin/qbank/QuestionPreviewCards.tsx
    - apps/web/src/pages/AdminQuestions.tsx
  </read_first>
  <files>apps/web/src/lib/exam.ts;apps/web/src/lib/student.ts;apps/web/src/pages/AdminExams.tsx;apps/web/src/pages/AdminExamDetail.tsx;apps/web/src/pages/AdminDashboard.tsx;apps/web/src/pages/StudentExamTake.tsx;apps/web/src/router.tsx;apps/web/src/components/auth/StudentRoute.tsx;apps/web/src/components/ui/radio-group.tsx;apps/web/src/components/ui/checkbox.tsx;apps/web/src/components/ui/alert-dialog.tsx</files>
  <action>
    **`lib/exam.ts`：** 镜像 `lib/roster.ts` 的 `apiFetch`；`listExams`, `createExam`, `getExam`, `startExam` 等。

    **`AdminExams.tsx`（04-UI-SPEC S1–S2）：** 列表 + 新建 Dialog（Select 两批次）；模式对齐 `AdminRoster.tsx` 表格/Card。

    **`AdminExamDetail.tsx`（S3–S4 部分，无成绩表/导出）：** 配置摘要、**开始考试** AlertDialog（**D-02**，调 04-01 API）、题目只读预览（**含** answerKeys 供教师核对）；模式对齐 `AdminQuestions.tsx` Dialog。

    **`AdminDashboard`：** 考试卡片 `href: '/admin/exams'`。`router.tsx` 注册 `exams`, `exams/:examId`；`StudentRoute` 允许 `/exam/take`。

    **`StudentExamTake.tsx`（S7，PATTERNS 类比 `QuestionPreviewCards` + `StudentLogin` 错误处理）：** query `examId`；`GET paper`；单页 Card；SINGLE/JUDGE `RadioGroup`，MULTI `Checkbox` + 多选说明；debounce **2000ms** `PUT answers` +「已保存」；底部「提交试卷」占位（Task 4 启用）。安装 shadcn `radio-group`, `checkbox`, `alert-dialog`。
  </action>
  <verify>
    <automated>pnpm --filter @lan-exam/web exec tsc -b --noEmit</automated>
    <automated>rg -q "AdminExams\|/admin/exams" apps/web/src/router.tsx</automated>
    <automated>rg -q "StudentExamTake\|RadioGroup\|saveAnswers" apps/web/src/pages/StudentExamTake.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `/admin/exams` 可创建考试并跳转详情
    - 详情页「开始考试」调用 `POST .../start` 成功
    - `/exam/take?examId=` 渲染题目且 Network 见 debounced `PUT .../answers` 200
    - `pnpm --filter @lan-exam/web exec tsc -b --noEmit` 退出码 0
  </acceptance_criteria>
  <done>EXAM-01 管理端与答题 UI 就绪；提交/成绩表在 Task 4。</done>
</task>

<task type="auto">
  <name>Task 4: 学生提交 UI 与管理端成绩表</name>
  <read_first>
    - .planning/phases/04-exam-submit-export/04-UI-SPEC.md
    - .planning/phases/04-exam-submit-export/04-ACCEPTANCE.md
    - apps/web/src/pages/StudentExamTake.tsx
    - apps/web/src/pages/AdminExamDetail.tsx
    - apps/web/src/lib/student.ts
  </read_first>
  <files>apps/web/src/lib/student.ts;apps/web/src/pages/StudentExamTake.tsx;apps/web/src/pages/AdminExamDetail.tsx</files>
  <action>
    **`studentApi`：** 增加 `submit(examId)`, `submission(examId)`；409 映射「您已提交过本场考试，无法再次提交。」

    **`StudentExamTake.tsx`：** 启用「提交试卷」+ `AlertDialog`；提交成功显示总分；`GET submission` 已存在 → 只读 + Banner（展示 `isCorrect`/`pointsAwarded`，**不** 展示标准答案）；409 结束考试 Alert。

    **`AdminExamDetail.tsx`（S4/S5）：** **结束考试**（**D-04**）；成绩表 + `maskNationalId`；未提交「—」与 Badge；导出按钮留给 04-03。
  </action>
  <verify>
    <automated>pnpm --filter @lan-exam/web exec tsc -b --noEmit</automated>
    <automated>rg -q "submit\|AlertDialog" apps/web/src/pages/StudentExamTake.tsx; rg -q "结束考试" apps/web/src/pages/AdminExamDetail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 学生提交后界面进入只读且再次点提交显示 EXAM-02 文案
    - 管理端成绩表显示已提交与未提交行
    - 教师「结束考试」后学生保存/提交被拒绝
  </acceptance_criteria>
  <done>EXAM-01/EXAM-02 与 D-04 前端闭环；导出留给 04-03。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST submit | 计分仅服务端；客户端 totalScore 忽略 |
| GET submission | 仅返回 session 对应 rosterEntryId 的记录 |
| Admin submissions | requireAdminSession + exam 归属 teacherId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | submit | mitigate | `@@unique([examId, rosterEntryId])` + 事务前检查 |
| T-04-02-02 | Tampering | PUT after submit | mitigate | submit 后拒绝 draft；删 drafts |
| T-04-02-03 | Repudiation | score | accept | 持久化 `Answer` 行含 `selectedKeys`, `pointsAwarded` |
| T-04-02-04 | Elevation | GET admin submission | mitigate | 校验 exam.teacherId === session teacher |
| T-04-02-05 | Denial of service | POST submit | mitigate | rateLimit 10/min |

</threat_model>

<verification>
- `node --import tsx --test apps/server/src/lib/exam/score-question.test.ts`
- 手工：submit → 409 再交 → teacher end → PUT 409
</verification>

<success_criteria>
- 计分单测与 02-ACCEPTANCE QBANK-02 表一致
- EXAM-02 重复提交 409 可演示
- D-04 结束考试后学生无法继续作答
</success_criteria>

<output>
After completion, create `.planning/phases/04-exam-submit-export/04-02-SUMMARY.md`
</output>
