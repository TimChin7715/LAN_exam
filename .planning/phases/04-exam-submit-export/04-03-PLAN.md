---
phase: 04-exam-submit-export
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - apps/server/src/lib/exam/export-workbook.ts
  - apps/server/src/lib/exam/mask-national-id.ts
  - apps/server/src/routes/api/admin/exams-export.ts
  - apps/server/src/index.ts
  - apps/web/src/lib/exam.ts
  - apps/web/src/pages/AdminExamDetail.tsx
  - apps/web/src/components/ui/tooltip.tsx
autonomous: true
requirements:
  - EXPR-01
  - EXPR-02
must_haves:
  truths:
    - "Teacher can download one xlsx with sheet 成绩汇总 containing name, masked ID, total score, submit status"
    - "Same file has sheet 答题明细 with per-question selected, correct answer, right/wrong, points"
    - "Unsubmitted roster entries appear in summary with 未提交 and empty score"
    - "Export works for ENDED exam with zero submissions; national ID never full 18 digits in file"
  artifacts:
    - path: apps/server/src/lib/exam/export-workbook.ts
      provides: exceljs dual-sheet workbook
      exports: buildExamExportWorkbook
    - path: apps/server/src/routes/api/admin/exams-export.ts
      provides: GET export stream
    - path: apps/web/src/pages/AdminExamDetail.tsx
      provides: 导出成绩与明细 button
  key_links:
    - from: apps/web/src/pages/AdminExamDetail.tsx
      to: GET /api/admin/exams/:id/export
      via: fetch blob download
      pattern: exams/.*/export
    - from: apps/server/src/lib/exam/export-workbook.ts
      to: prisma.submission + rosterEntry
      via: left join all roster in batch
      pattern: maskNationalId
---

## Phase Goal

**As a** 监考教师, **I want to** 下载含成绩汇总与答题明细的 Excel 存档, **so that** 满足校方成绩归档与逐题审计（EXPR-01 + EXPR-02）。

<objective>
交付 **导出垂直切片**：**exceljs** 单文件双工作表（RESEARCH 默认）、证号 **脱敏**、未提交考生出现在汇总表；管理端一键下载（04-UI-SPEC S5）。
</objective>

<execution_context>
@E:/programs/LAN_exam/.cursor/get-shit-done/workflows/execute-plan.md
@E:/programs/LAN_exam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-exam-submit-export/04-RESEARCH.md
@.planning/phases/04-exam-submit-export/04-UI-SPEC.md
@.planning/PROJECT.md
@apps/server/src/routes/api/admin/roster-template.ts
@apps/server/scripts/generate-roster-template.ts
@apps/web/src/lib/roster.ts

<interfaces>
maskNationalId (server `mask-national-id.ts`, align roster.ts):
- 18位: slice(0,6) + '********' + slice(-4)
- 非18位: '—'

export columns:
- 成绩汇总: 姓名, 身份证号(脱敏), 总分, 是否提交, 提交时间
- 答题明细: 姓名, 身份证号(脱敏), 题号, 题型, 所选, 正确答案, 对错, 得分

GET /api/admin/exams/:id/export → stream xlsx, Content-Disposition attachment filename UTF-8
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: export-workbook 与 maskNationalId</name>
  <read_first>
    - apps/server/scripts/generate-roster-template.ts
    - apps/server/src/routes/api/admin/roster-template.ts
    - .planning/phases/04-exam-submit-export/04-RESEARCH.md
    - apps/web/src/lib/roster.ts
  </read_first>
  <files>apps/server/src/lib/exam/mask-national-id.ts;apps/server/src/lib/exam/export-workbook.ts</files>
  <action>
    **`mask-national-id.ts`：** `export function maskNationalId(id: string): string` — 与 `apps/web/src/lib/roster.ts` `maskId` 语义一致（18 位前6+8星+后4；否则「—」）。

    **`export-workbook.ts`：** `buildExamExportWorkbook(examId)` 使用 ExcelJS：Sheet1 **成绩汇总** — 遍历 `exam.rosterBatch` 下全部 `RosterEntry` left join `Submission`；未提交行：总分空或「—」、是否提交「未提交」、提交时间空（ACCEPTANCE：**空总分 + 未提交标志**）。Sheet2 **答题明细** — 仅已提交考生的每题 `Answer` 一行：题号用 `ExamQuestion.sortOrder+1`、题型中文、所选、正确答案、对错（是/否）、得分。证号列一律 `maskNationalId`。**禁止** 日志打印完整证号。

    **性能：** 使用 `workbook.xlsx.write(stream)` 流式写入；v1 上限写在代码注释（2000人×200题）即可。

    **单元（轻量）：** 可对 `maskNationalId('11010519491231002X')` 做断言测试（可选同文件 test 或 inline verify）。
  </action>
  <verify>
    <automated>pnpm --filter @lan-exam/server exec tsc -b --noEmit</automated>
    <automated>rg -q "成绩汇总" apps/server/src/lib/exam/export-workbook.ts; rg -q "maskNationalId" apps/server/src/lib/exam/export-workbook.ts</automated>
  </verify>
  <acceptance_criteria>
    - `export-workbook.ts` 存在且引用 `exceljs`
    - 汇总 sheet 表头含「是否提交」
    - 明细 sheet 表头含「所选」「正确答案」「得分」
    - 无 `nationalId` 全量 18 位写入 xlsx 的代码路径（仅 mask 后）
  </acceptance_criteria>
  <done>导出数据层就绪。</done>
</task>

<task type="auto">
  <name>Task 2: GET export 路由与注册</name>
  <read_first>
    - apps/server/src/routes/api/admin/roster-template.ts
    - apps/server/src/plugins/admin-guard.ts
    - apps/server/src/routes/api/admin/exams-crud.ts
  </read_first>
  <files>apps/server/src/routes/api/admin/exams-export.ts;apps/server/src/index.ts</files>
  <action>
    **`exams-export.ts`：** `GET /api/admin/exams/:id/export` — `requireAdminSession`；校验 exam 存在且 `teacherId` 归属当前教师；`buildExamExportWorkbook`；设置 `Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`；`Content-Disposition: attachment; filename*=UTF-8''{encodeURIComponent(title)}-成绩与明细.xlsx`；`reply.send(stream)` 或 `write(reply.raw)`。**DRAFT** 且无提交时仍允许导出（空汇总+空明细或仅名单行，UI-SPEC S5）。**禁止** 缓存敏感响应到公共 CDN（内网无此需求，不设置 public cache）。

    在 `apps/server/src/index.ts` 注册 `registerAdminExamsExportRoutes`。
  </action>
  <verify>
    <automated>rg -q "exams-export\|/export" apps/server/src/index.ts</automated>
    <automated>pnpm --filter @lan-exam/server exec tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 路由路径为 `/api/admin/exams/:id/export`
    - 未登录返回 401
    - 响应 Content-Type 为 xlsx
  </acceptance_criteria>
  <done>教师可通过 API 下载导出文件。</done>
</task>

<task type="auto">
  <name>Task 3: 管理端导出按钮与下载（垂直切片）</name>
  <read_first>
    - .planning/phases/04-exam-submit-export/04-UI-SPEC.md
    - apps/web/src/lib/roster.ts
    - apps/web/src/pages/AdminExamDetail.tsx
  </read_first>
  <files>apps/web/src/lib/exam.ts;apps/web/src/pages/AdminExamDetail.tsx;apps/web/src/components/ui/tooltip.tsx</files>
  <action>
    **`lib/exam.ts`：** `downloadExamExport(examId, title)` — 镜像 `downloadRosterTemplate`：`fetch` credentials include → blob → `<a download>`；文件名 `{title}-成绩与明细.xlsx`；401 toast「登录已过期」。

    **`AdminExamDetail.tsx`（S5）：** Primary outline「导出成绩与明细」；`DRAFT` 无提交时 disabled + `Tooltip`「请先开始考试并有考生提交」；`ENDED` 始终可点；下载中 loading 态。

    安装 shadcn `tooltip`（若缺失）。

    **验收辅助：** 导出后用 Excel/WPS 打开确认两 sheet 名称与中文表头；抽样证号为脱敏格式。
  </action>
  <verify>
    <automated>pnpm --filter @lan-exam/web exec tsc -b --noEmit</automated>
    <automated>rg -q "downloadExamExport\|导出成绩" apps/web/src</automated>
  </verify>
  <acceptance_criteria>
    - 详情页存在「导出成绩与明细」按钮
    - `lib/exam.ts` 含 blob 下载逻辑
    - ENDED 考试点击可触发下载请求
  </acceptance_criteria>
  <done>EXPR-01/EXPR-02 UI 闭环。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET export | 仅已登录教师；exam 归属校验 |
| xlsx 文件 | 离开系统到教师本机 — 脱敏降低泄露面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information disclosure | export xlsx | mitigate | maskNationalId 全表；不导出完整证号 |
| T-04-03-02 | Elevation | exams-export | mitigate | requireAdminSession + teacherId match |
| T-04-03-03 | Information disclosure | logs | mitigate | 日志仅 examId/rosterEntryId |
| T-04-03-04 | Denial of service | large export | accept | v1 单考场规模；流式写；ACCEPTANCE 规模上限 |

</threat_model>

<verification>
- 教师登录后 `curl -I -b cookies http://localhost:3001/api/admin/exams/{id}/export` 见 xlsx Content-Type
- 打开文件：两 sheet、未提交行、明细行数 = 提交人数 × 题数
- `rg -n "nationalId" apps/server/src/lib/exam/export-workbook.ts` 确认写入列为 mask 后
</verification>

<success_criteria>
- EXPR-01 汇总字段齐全且可存档
- EXPR-02 明细逐题可核对
- 导出证号均为脱敏形态
</success_criteria>

<output>
After completion, create `.planning/phases/04-exam-submit-export/04-03-SUMMARY.md`
</output>
