---
phase: 02-qbank-import
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/**/*
  - apps/server/package.json
  - apps/server/src/index.ts
  - apps/server/src/lib/qbank/**/*
  - apps/server/src/routes/api/admin/questions-template.ts
  - apps/server/src/routes/api/admin/questions-import.ts
  - apps/server/src/routes/api/admin/questions-list.ts
autonomous: true
requirements:
  - QBANK-01
  - QBANK-02
  - QBANK-03
must_haves:
  truths:
    - "POST import with valid template xlsx returns 200 and creates QuestionImportBatch with nested questions in one transaction"
    - "POST import with any row error returns 400 with row-level errors and zero new Question rows"
    - "GET template streams docs/templates/题库导入模板.xlsx with attachment disposition"
    - "Imported MULTI questions persist multiScoringRule ALL_OR_NOTHING per D-07 and research decision"
  artifacts:
    - prisma/schema.prisma
    - apps/server/src/lib/qbank/parse-workbook.ts
    - apps/server/src/lib/qbank/validate-rows.ts
    - apps/server/src/lib/qbank/import-questions.ts
    - apps/server/src/routes/api/admin/questions-import.ts
  key_links:
    - "questions-import route uses requireAdminSession preHandler same pattern as apps/server/src/routes/api/admin/ping.ts"
    - "import reads sheet name 题库导入 per D-02"
---

<objective>
交付 **题库导入后端管道**：Prisma 题目模型、ExcelJS 解析与行级校验、全批校验通过后单次事务写入；官方模板下载 API。满足 ROADMAP 成功标准 #2（非法文件零入库）与 QBANK-01～03 的服务端契约（**D-01～D-10**）。
</objective>

<execution_context>
@E:/programs/LAN_exam/.cursor/get-shit-done/workflows/execute-plan.md
@E:/programs/LAN_exam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-qbank-import/02-CONTEXT.md
@.planning/phases/02-qbank-import/02-RESEARCH.md
@docs/templates/题库导入模板.xlsx
@apps/server/src/plugins/admin-guard.ts
@apps/server/src/routes/api/admin/ping.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma 题目域模型与迁移</name>
  <read_first>
    - .planning/phases/02-qbank-import/02-CONTEXT.md
    - .planning/phases/02-qbank-import/02-RESEARCH.md
    - prisma/schema.prisma
    - .planning/phases/01-foundation-auth/01-02-PLAN.md
  </read_first>
  <files>prisma/schema.prisma;prisma/migrations/**/*</files>
  <action>
    扩展 `prisma/schema.prisma`：**D-01/D-09**。新增枚举 `QuestionType`（SINGLE, MULTI, JUDGE）、`MultiScoringRule`（至少 `ALL_OR_NOTHING`）。`QuestionImportBatch`：`id`, `teacherId`→`Teacher`, `fileName`, `totalRows`, `importedCount`, `skippedCount`（默认 0）, `createdAt`。`Question`：`batchId`, `type`, `stem`, `answerKeys`（规范化字符串，单选 `A`，多选 `A,B,C` 排序后逗号分隔）, `points`（默认 1，**D-08**）, `difficulty`（默认 1，1–5）, `explanation?`, `knowledgePoints?`（MVP 存原始字符串）, `multiScoringRule?`（仅 MULTI 必填 `ALL_OR_NOTHING`，**QBANK-02**）。`QuestionOption`：`questionId`, `key`（A–Z）, `text`, `sortOrder`，`@@unique([questionId, key])`。**不**建警种字段（**D-09**）。`Teacher` 增加 `questionImportBatches` 关系。
  </action>
  <verify>`pnpm exec prisma validate` 退出码 0。</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` 含 `Question`, `QuestionOption`, `QuestionImportBatch` 与两枚举
    - `Question.multiScoringRule` 类型为 `MultiScoringRule?`
    - 迁移 SQL 创建上述表与外键
  </acceptance_criteria>
  <done>模型与迁移文件就绪，待 Task 3 应用。</done>
</task>

<task type="auto">
  <name>Task 2: Excel 解析、校验与事务导入库</name>
  <read_first>
    - .planning/phases/02-qbank-import/02-RESEARCH.md
    - docs/templates/题库导入模板.xlsx
    - apps/server/src/lib/prisma.ts
  </read_first>
  <files>apps/server/package.json;apps/server/src/lib/qbank/parse-workbook.ts;apps/server/src/lib/qbank/validate-rows.ts;apps/server/src/lib/qbank/normalize-answer.ts;apps/server/src/lib/qbank/import-questions.ts</files>
  <action>
    `pnpm --filter @lan-exam/server add exceljs @fastify/multipart`。在 `apps/server/src/lib/qbank/` 实现：**D-02** 仅解析 sheet「题库导入」（无则 400 `INVALID_TEMPLATE`）；**D-03** 表头 trim 匹配；**D-05** 跳过题干以 `【示例】` 开头；**D-04** 题型映射 single/单选→SINGLE 等；**D-06** 判断题 A/B 空则补「正确」「错误」；**D-07** 答案规范化（多选 `split(/[,，、\s]+/)` 大写字母去重排序）；**D-08** 难度/分值缺省；**D-09** 忽略警种列。`validate-rows` 返回 `{ questions, errors }`；`errors.length > 0` 时调用方不得写库。`import-questions` 使用 `prisma.$transaction` 嵌套 create batch+questions+options（追加策略，无覆盖，**RESEARCH**）。行数上限 **2000**，空行跳过。
  </action>
  <verify>对仓库模板 xlsx 的单元测试或脚本：合法 3 条示例行（跳过【示例】后实际导入条数以模板为准）解析成功；故意错答案行产生 `errors` 且不含 `questions`。</verify>
  <acceptance_criteria>
    - `apps/server/src/lib/qbank/parse-workbook.ts` 导出 `parseWorkbook(buffer: Buffer)`
    - `validate-rows.ts` 对非法题型/缺题干/答案不在选项内返回带 `rowNumber` 的 error
    - `import-questions.ts` 仅在 `errors` 为空时调用 `$transaction`
  </acceptance_criteria>
  <done>纯函数层可单测；尚未挂路由。</done>
</task>

<task type="auto">
  <name>Task 3: Admin API — 模板、导入、列表、详情</name>
  <read_first>
    - apps/server/src/index.ts
    - apps/server/src/plugins/admin-guard.ts
    - apps/server/src/routes/api/admin/ping.ts
    - .planning/phases/02-qbank-import/02-RESEARCH.md
  </read_first>
  <files>apps/server/src/index.ts;apps/server/src/routes/api/admin/questions-template.ts;apps/server/src/routes/api/admin/questions-import.ts;apps/server/src/routes/api/admin/questions-list.ts</files>
  <action>
    注册 `@fastify/multipart`：`limits.fileSize: 5*1024*1024`, `files: 1`。**D-10** `GET /api/admin/questions/template`：`createReadStream` 读 `docs/templates/题库导入模板.xlsx`，`Content-Disposition: attachment; filename*=UTF-8''%E9%A2%98%E5%BA%93%E5%AF%BC%E5%85%A5%E6%A8%A1%E6%9D%BF.xlsx`，`preHandler: requireAdminSession`。`POST /api/admin/questions/import`：字段名 `file`，校验 mimetype/扩展名/PK 魔数；`toBuffer()`→解析→校验→导入或 `400 { ok:false, errors:[{row,column,message}] }`；成功 `200 { ok:true, batchId, importedCount, skippedCount, previewQuestions: 前3题摘要 }`。**GET /api/admin/questions`：**Zod** `page`, `pageSize`（默认20）, `type?`, `batchId?`；**GET /api/admin/questions/:id`** 含 options 排序。所有路由 `requireAdminSession`。
  </action>
  <verify>已登录教师 cookie 下 `curl -b` 或 vitest/supertest：template 200；合法 xlsx import 200；坏文件 400 且 DB 题目数不变。</verify>
  <acceptance_criteria>
    - `apps/server/src/index.ts` 注册三条 admin questions 路由
    - `POST /api/admin/questions/import` 响应 JSON 含 `errors` 数组或 `batchId`
    - 未登录请求返回 401/403 与 Phase 1 一致
  </acceptance_criteria>
  <done>后端 API 满足 QBANK-01～03 导入能力（无前端）。</done>
</task>

<task type="blocking">
  <name>Task 4: [BLOCKING] Prisma migrate 应用 schema</name>
  <read_first>
    - prisma/schema.prisma
    - docker-compose.yml
    - docs/DEPLOY.md
  </read_first>
  <files>prisma/migrations/**/*</files>
  <action>
    在开发环境执行 `pnpm exec prisma migrate dev --name qbank_questions`（非交互失败时设 `CI=true` 或使用 `--create-only` 后 `migrate deploy`）。Compose 部署路径确保 `prisma migrate deploy` 包含本迁移。文档无需重复，仅确认迁移目录已提交。
  </action>
  <verify>`pnpm exec prisma migrate deploy` 对空库/已有 Phase 1 库退出码 0；`pnpm exec prisma generate` 成功。</verify>
  <acceptance_criteria>
    - `prisma/migrations` 存在本 plan 对应迁移
    - 数据库中存在 `Question`, `QuestionOption`, `QuestionImportBatch` 表
  </acceptance_criteria>
  <done>类型与 DB 与 schema 一致，避免验证假阳性。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 教师浏览器 → multipart | 不可信文件；仅服务端解析 |
| 教师 Session → admin API | 须 `requireAdminSession` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Mitigation Plan |
|-----------|----------|-----------|----------|-----------------|
| T-02-01 | E | POST import 无会话 | high | `requireAdminSession` on all routes |
| T-02-02 | D | 超大 xlsx | high | 5MB + 2000 行 + rate-limit |
| T-02-03 | T | MIME 伪造 | medium | 扩展名 + mimetype + ZIP 魔数 |
| T-02-04 | I | 400 响应含堆栈 | medium | 仅 `{row,column,message}` |
| T-02-05 | T | 部分写入脏数据 | high | 校验失败零 `prisma.create` |

</threat_model>

<verification>
- 使用仓库 `docs/templates/题库导入模板.xlsx` 导入后 DB 含 SINGLE/MULTI/JUDGE 各至少一题（若模板仅示例行被跳过则用测试 fixture）。
- 错误 xlsx 后 `SELECT count(*) FROM "Question"` 与导入前相同。
</verification>

<success_criteria>
题目域持久化与导入 API 可用；QBANK-02 计分规则 `ALL_OR_NOTHING` 已入库；为 02-02/02-03 前端提供稳定契约。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-qbank-import/02-01-SUMMARY.md`
</output>
