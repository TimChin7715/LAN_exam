---
phase: 02-qbank-import
plan: 02-03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - apps/web/src/pages/AdminQuestions.tsx
  - apps/web/src/components/admin/qbank/QuestionDetailDialog.tsx
  - apps/web/src/components/admin/qbank/ImportResultSummary.tsx
  - docs/DEPLOY.md
  - .planning/phases/02-qbank-import/02-ACCEPTANCE.md
autonomous: true
requirements:
  - QBANK-02
must_haves:
  truths:
    - "Imported MULTI questions display multiScoringRule ALL_OR_NOTHING in UI with fixed Chinese explanation per QBANK-02"
    - "Multi-select answer in preview and dialog matches normalized comma-separated keys from import"
    - "Phase 2 acceptance doc states scoring rule for Phase 4 consumers"
    - "List filter by type 多选 shows only MULTI rows"
  artifacts:
    - apps/web/src/components/admin/qbank/QuestionDetailDialog.tsx
    - .planning/phases/02-qbank-import/02-ACCEPTANCE.md
  key_links:
    - "Dialog reads multiScoringRule from GET /api/admin/questions/:id JSON"
    - "02-ACCEPTANCE.md documents ALL_OR_NOTHING definition"
---

<objective>
**垂直切片（多选题）：** 在 02-02 壳层上完善多选导入的可见验收——列表筛选、详情 Dialog 展示 **全对满分否则 0 分** 规则说明（**QBANK-02**），并写入阶段验收文档供 Phase 4 计分复用。确保混合 xlsx 一次导入后多选题预览/详情与源文件答案一致。
</objective>

<execution_context>
@E:/programs/LAN_exam/.cursor/get-shit-done/workflows/execute-plan.md
@E:/programs/LAN_exam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-qbank-import/02-CONTEXT.md
@.planning/phases/02-qbank-import/02-RESEARCH.md
@.planning/phases/02-qbank-import/02-UI-SPEC.md
@apps/web/src/pages/AdminQuestions.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 多选详情 Dialog 与计分规则展示</name>
  <read_first>
    - .planning/phases/02-qbank-import/02-UI-SPEC.md
    - .planning/phases/02-qbank-import/02-RESEARCH.md
    - apps/web/src/pages/AdminQuestions.tsx
  </read_first>
  <files>apps/web/src/components/admin/qbank/QuestionDetailDialog.tsx;apps/web/src/pages/AdminQuestions.tsx</files>
  <action>
    抽离或扩展 `QuestionDetailDialog`：当 `type === 'MULTI'` 时，在答案下方显示固定说明块（`text-sm text-muted-foreground`）：「计分规则：全对满分，否则 0 分（选项须与正确答案完全一致，多选、少选、错选均不得分）。」数据字段 `multiScoringRule === 'ALL_OR_NOTHING'` 时显示；若 API 缺失该字段则同时显示 warning 文案供调试。多选答案渲染为标签组 `A` `B` `C`（来自 `answerKeys` 拆分）。单选/判断保持 02-02 展示不变。
  </action>
  <verify>导入含多选行的模板后打开 Dialog 见上述中文规则；`multiScoringRule` 在 JSON 中为 `ALL_OR_NOTHING`。</verify>
  <acceptance_criteria>
    - MULTI 详情含计分说明段落，文案与 PLAN 一致
    - 非 MULTI 题型不显示计分规则块
    - 答案标签与 `answerKeys` 一致（如 `A,B,C` → 三枚 badge）
  </acceptance_criteria>
  <done>QBANK-02 UI 验收可见。</done>
</task>

<task type="auto">
  <name>Task 2: 列表题型筛选与本批多选预览强化</name>
  <read_first>
    - .planning/phases/02-qbank-import/02-UI-SPEC.md
    - apps/web/src/components/admin/qbank/QuestionPreviewCards.tsx
  </read_first>
  <files>apps/web/src/pages/AdminQuestions.tsx;apps/web/src/components/admin/qbank/QuestionPreviewCards.tsx;apps/web/src/components/admin/qbank/ImportResultSummary.tsx</files>
  <action>
    列表上方增加 `Select` 题型筛选：全部 / 单选 / 多选 / 判断，映射 query `type=SINGLE|MULTI|JUDGE`。`QuestionPreviewCards` 对 MULTI 卡片角标「多选」并显示答案为多个字母。`ImportResultSummary` 成功文案补充：「多选题已按『全对满分，否则 0 分』规则入库。」（一句即可）。可选：批次筛选 `batchId` 默认最近一次导入（若 API 支持 `batchId` query）。
  </action>
  <verify>筛选「多选」仅显示 MULTI 行；导入成功后摘要含计分句。</verify>
  <acceptance_criteria>
    - `type=MULTI` 请求参数在筛选为「多选」时发出
    - 预览卡 MULTI 显示多个答案字母
    - 成功摘要含「全对满分，否则 0 分」短语
  </acceptance_criteria>
  <done>多选导入在列表与预览可辨认。</done>
</task>

<task type="auto">
  <name>Task 3: 阶段验收说明与部署备注</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - docs/DEPLOY.md
  </read_first>
  <files>.planning/phases/02-qbank-import/02-ACCEPTANCE.md;docs/DEPLOY.md</files>
  <action>
    新建 `02-ACCEPTANCE.md`：记录 Phase 2 三条 ROADMAP 成功标准核对方式；**QBANK-02** 锁定 `MultiScoringRule.ALL_OR_NOTHING` 定义（学员得分 = 选项集合与 `answerKeys` 完全一致则 `points`，否则 0）；注明 Phase 4 阅卷须读该字段。`docs/DEPLOY.md` 增加简短「题库导入」：依赖 `exceljs`、模板路径、建议首次用仓库模板 smoke test。
  </action>
  <verify>文件存在且含 `ALL_OR_NOTHING` 与「全对满分，否则 0 分」字样。</verify>
  <acceptance_criteria>
    - `02-ACCEPTANCE.md` 含 QBANK-01/02/03 可勾选项
    - `docs/DEPLOY.md` 含题库导入小节标题
  </acceptance_criteria>
  <done>计分规则对教师与后续阶段可见、可测。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 计分规则展示 | 只读说明；不在前端计算分数 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Mitigation Plan |
|-----------|----------|-----------|----------|-----------------|
| T-02-08 | R | 教师误解计分规则 | low | 固定文案 + ACCEPTANCE 文档 |
| T-02-09 | T | 客户端篡改 multiScoringRule | medium | 规则仅存 DB；Phase 4 服务端计分 |

</threat_model>

<verification>
- 使用模板中【示例】多选行（若跳过示例则用含多选的测试 xlsx）导入后：筛选多选 → Dialog 规则文案 → `answerKeys` 与 Excel 一致。
- `02-ACCEPTANCE.md` 与 REQUIREMENTS QBANK-02 对齐。
</verification>

<success_criteria>
QBANK-02 闭环：多选题可导入、规则固定为 ALL_OR_NOTHING、UI 与文档双重可见；Phase 2 三条 ROADMAP 成功标准均有手动验证步骤。
</success_criteria>

<output>
完成后创建 `.planning/phases/02-qbank-import/02-03-SUMMARY.md`
</output>
