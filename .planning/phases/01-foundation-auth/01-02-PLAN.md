---
phase: 01-foundation-auth
plan: 01-02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/**/*
  - prisma/seed.ts
  - apps/server/package.json
  - apps/server/src/lib/prisma.ts
  - docker-compose.yml
  - apps/server/src/index.ts
  - .env.example
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "prisma migrate deploy applies cleanly to empty database in CI or deploy script"
    - "prisma db seed inserts at least one Teacher with mustChangePassword true and argon2 password hash only"
    - "application startup performs successful database ping such as SELECT 1 via Prisma"
  artifacts:
    - prisma/schema.prisma
    - prisma/migrations
    - prisma/seed.ts
    - .env.example
  key_links:
    - "`DATABASE_URL` 在 `.env.example` 与 `docker-compose.yml` 的 `app` 服务环境中一致且指向 `db` 服务主机名"
---

<objective>
引入 **PostgreSQL + Prisma**：定义 `Teacher` 模型（支撑多教师 **D-01**）、`mustChangePassword`（**D-02**）、密码哈希字段；提供迁移与 **种子教师账号**；使 `apps/server` 在 Compose 下可连库并完成至少一次真实读（如启动检查或 `/health` 深度检查）。
</objective>

<execution_context>
@E:/programs/LAN_exam/.cursor/get-shit-done/workflows/execute-plan.md
@E:/programs/LAN_exam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-SKELETON.md
@apps/server/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema、迁移与 Teacher 表</name>
  <read_first>
    - .planning/phases/01-foundation-auth/01-CONTEXT.md
    - prisma/schema.prisma
    - docker-compose.yml
  </read_first>
  <files>prisma/schema.prisma;prisma/migrations/**/*;apps/server/package.json;apps/server/src/lib/prisma.ts;.env.example</files>
  <action>
    在仓库根添加 Prisma，`datasource db` 使用 PostgreSQL。`Teacher` 模型至少包含：`id`（cuid/uuid）、`username`（unique）、`passwordHash`（String）、`displayName`（String）、`mustChangePassword`（Boolean，默认 true）、`createdAt`、`updatedAt`。**D-01**：不设角色表；注释说明未来 RBAC 扩展点。配置 `prisma/seed.ts`：使用 argon2 对种子口令哈希后 upsert 至少一名 `teacher_admin`（用户名常量写入 seed，口令来自环境变量 `SEED_ADMIN_PASSWORD`，**禁止**硬编码进 git）。`.env.example` 列出 `DATABASE_URL` 与 `SEED_ADMIN_PASSWORD`。`apps/server` 内封装 `PrismaClient` 单例并在关闭钩子中断开。
  </action>
  <verify>`pnpm exec prisma validate` 退出码 0；`pnpm exec prisma migrate dev --name init_teacher`（开发）生成迁移目录。</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` 存在模型 `Teacher` 且 `username` 带 `@unique`
    - `prisma/migrations` 下至少一条迁移包含 `CREATE TABLE` 对应 Teacher
    - `.env.example` 含 `DATABASE_URL=` 与 `SEED_ADMIN_PASSWORD=`
  </acceptance_criteria>
  <done>迁移可应用；类型生成可用（若启用 prisma generate）。</done>
</task>

<task type="auto">
  <name>Task 2: Compose 集成 migrate + seed 与启动顺序</name>
  <read_first>
    - docker-compose.yml
    - Dockerfile
    - docs/DEPLOY.md
  </read_first>
  <files>docker-compose.yml;Dockerfile;docs/DEPLOY.md;apps/server/src/index.ts</files>
  <action>
    更新 `docker-compose.yml`：`app` 服务在启动命令前运行 `prisma migrate deploy && prisma db seed`（或独立 entrypoint 脚本 `scripts/docker-entrypoint.sh`）。确保 `depends_on` 使用 Postgres `service_healthy`。更新 `docs/DEPLOY.md`：说明首次部署须设置 `SEED_ADMIN_PASSWORD`、如何在局域网内验证 DB 就绪（`docker compose logs` 或 `psql`）。在 `apps/server` 启动时执行一次轻量 DB ping（例如 `await prisma.$queryRaw\`SELECT 1\``）失败则进程退出非零，避免「假健康」。
  </action>
  <verify>`docker compose build` 成功（在用户环境允许 Docker 的前提下）；若无法运行 Docker，则本地 `pnpm exec prisma migrate deploy` + `pnpm exec prisma db seed` 对本地 Postgres 成功。</verify>
  <acceptance_criteria>
    - `docker-compose.yml` 中 `app` 服务环境变量传入 `DATABASE_URL`
    - 文档描述种子账号的**用户名**与**首登改密**提示（指向下一 PLAN）
    - 启动脚本或文档中明确迁移失败时容器应退出（非 0）
  </acceptance_criteria>
  <done>数据库层与 INFRA-01 的数据持久化前提打通。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 运维 → `.env` / Compose env | 高敏感；不入库 git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I | `SEED_ADMIN_PASSWORD` 泄露 | mitigate | 文档强调仅首次部署注入；生产用机密管理；日志禁止打印 |
| T-02-02 | T | 迁移文件被篡改 | mitigate | 迁移纳入 PR 审查；CI 校验 checksum（可选） |
| T-02-03 | E | DB 暴露端口 | mitigate | Compose 默认仅内部网络暴露 Postgres，不把 5432 绑到 `0.0.0.0` 除非文档明确需要 |
</threat_model>

<verification>
- 空库上 `migrate deploy` + `db seed` 后，`Teacher` 表至少一行。
- 应用启动 DB ping 成功。
</verification>

<success_criteria>
持久化层可用，种子教师存在，满足 Walking Skeleton 中「真实 DB 读/写」前提（完整登录写操作在 01-03）。
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
