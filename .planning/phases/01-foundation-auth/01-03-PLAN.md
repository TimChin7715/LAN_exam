---
phase: 01-foundation-auth
plan: 01-03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - apps/server/src/index.ts
  - apps/server/src/routes/api/auth/**/*.ts
  - apps/server/src/routes/api/admin/**/*.ts
  - apps/server/src/plugins/session.ts
  - apps/web/src/**/*.tsx
  - apps/web/src/lib/api.ts
  - apps/web/src/router.tsx
  - prisma/schema.prisma
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "POST /api/auth/login sets HttpOnly session cookie and JSON includes mustChangePassword and displayName on success"
    - "GET /api/auth/me returns 401 without cookie and 200 with user fields when session valid"
    - "POST /api/auth/change-password clears mustChangePassword flag so /api/admin routes become reachable"
    - "GET /api/admin/ping returns 401 when unauthenticated"
    - "SPA routes match 01-UI-SPEC S1 through S4 including redirect query checking copy and invalid credential copy per UI-SPEC"
  artifacts:
    - apps/server/src/routes/api/auth/login.ts
    - apps/web/src/pages/AdminLogin.tsx
    - apps/web/src/pages/AdminChangePassword.tsx
    - apps/web/src/pages/AdminDashboard.tsx
  key_links:
    - "前端所有 `fetch` 对同源 API 使用 `credentials: 'include'`"
---

<objective>
实现 **AUTH-01**：教师凭据登录管理端；**服务端 Session + HttpOnly Cookie（D-04）**；**首登强制改密（D-02）**；**无忘记密码入口（D-03）**；**多教师平等账号模型（D-01）**；管理端与学生占位 UI 严格对齐 **`01-UI-SPEC.md`**；补充 **D-07** 结构化控制台日志（登录成功/失败不写明文密码）。
</objective>

<execution_context>
@E:/programs/LAN_exam/.cursor/get-shit-done/workflows/execute-plan.md
@E:/programs/LAN_exam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-SKELETON.md
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: 会话中间件与认证 API（login / logout / me / change-password）</name>
  <read_first>
    - .planning/phases/01-foundation-auth/01-CONTEXT.md
    - .planning/phases/01-foundation-auth/01-UI-SPEC.md
    - prisma/schema.prisma
    - apps/server/src/index.ts
  </read_first>
  <files>apps/server/src/index.ts;apps/server/src/plugins/session.ts;apps/server/src/routes/api/auth/login.ts;apps/server/src/routes/api/auth/logout.ts;apps/server/src/routes/api/auth/me.ts;apps/server/src/routes/api/auth/change-password.ts;apps/server/src/routes/api/admin/ping.ts</files>
  <action>
    集成 **`express-session` + `connect-pg-simple`** 将 session 存 PostgreSQL（与 RESEARCH 一致）；Cookie 名建议 `sid`，属性：`httpOnly: true`、`sameSite: 'lax'`（同域部署）、`secure` 在 `NODE_ENV=production` 且 `TRUST_PROXY=true` 时为 true（与 DEPLOY 文档一致）**[ASSUMED]**。实现路由：`POST /api/auth/login` body `{ username, password }`，成功时 `regenerate` session 防 fixation，写入 `teacherId`；失败时 **401** 且 JSON `code: INVALID_CREDENTIALS`，日志与响应文案均不得区分用户不存在与密码错误（**AUTH-01**）。`POST /api/auth/logout` 销毁会话。`GET /api/auth/me` 返回 `username`、`displayName`、`mustChangePassword`。`POST /api/auth/change-password` body `{ currentPassword, newPassword }`：校验当前密码、argon2 哈希新密码、将 `mustChangePassword` 置 `false`；若当前密码错误返回 **400** 且消息与 UI-SPEC 一致键名供前端映射。实现 `preHandler` 守卫：`/api/admin/*`（除白名单）要求已登录且 `mustChangePassword=false`，否则 **401**。对 `POST /api/auth/login` 挂载 `@fastify/rate-limit`（例如每 IP 15 次/分钟，可配置）缓解暴力破解。密码校验使用 `argon2.verify`。
  </action>
  <verify>使用 `curl` 或 `httpie`：`POST /api/auth/login` 错误密码返回 401；正确密码返回 200 且带 `Set-Cookie`；随后 `GET /api/auth/me` 带 Cookie 返回 200。</verify>
  <acceptance_criteria>
    - 源码中存在路由前缀 `/api/auth/login` 与 `/api/auth/change-password`
    - `apps/server` 依赖中包含 `express-session` 与 `connect-pg-simple` 与 `@fastify/rate-limit`
    - 401 响应体**不包含**子串 `用户不存在` 或 `密码错误` 的互斥提示（仅允许统一文案字段）
    - 结构化日志在登录失败时包含 `event: auth_login_failed` 且不包含请求体内的 `password` 原文
  </acceptance_criteria>
  <done>后端认证与授权边界符合 CONTEXT D-01～D-04 与 ASVS L1 基线。</done>
</task>

<task type="auto">
  <name>Task 2: 管理端 SPA 页面、路由守卫与 / 占位</name>
  <read_first>
    - .planning/phases/01-foundation-auth/01-UI-SPEC.md
    - apps/web/vite.config.ts
    - apps/web/src/main.tsx
  </read_first>
  <files>apps/web/src/router.tsx;apps/web/src/lib/api.ts;apps/web/src/pages/Home.tsx;apps/web/src/pages/AdminLogin.tsx;apps/web/src/pages/AdminChangePassword.tsx;apps/web/src/pages/AdminDashboard.tsx;apps/web/src/components/admin/AdminLayout.tsx;apps/web/src/globals.css</files>
  <action>
    使用 React Router（或等价）实现路径：`/` 学生占位文案 **精确** 为「局域网考试系统 — 学生端即将开放」；`/admin/login`、`/admin/change-password`、`/admin` 与可选 `/admin/dashboard` 合并为仪表盘壳层。按 UI-SPEC 安装 shadcn 组件：`button`、`input`、`label`、`card`、`alert`、`form`（react-hook-form+zod）、`sonner`、spinner。实现 S4 四态矩阵：`checking` 全屏 Spinner +「正在验证登录状态…」；未登录访问 `/admin/*`（除 login）重定向至 `/admin/login?redirect=`；`mustChangePassword` 重定向改密页；API **401** 时 toast「登录已过期，请重新登录。」并清本地假设后跳转登录。登录页主按钮文案为「登录管理端」；错误 Alert 文案为「用户名或密码错误，请检查后重试。」；**不得渲染**「忘记密码」。仪表盘顶栏左侧「局域网考试系统」、右侧用户名 +「退出登录」；占位区含 3 个 **disabled** 预告卡片文案与 UI-SPEC「即将开放」一致。`api.ts` 封装 `fetch` 默认 `credentials: 'include'`。将 UI-SPEC 颜色变量写入 `globals.css`（slate/zinc/blue/red 取值与规范表一致）。
  </action>
  <verify>在浏览器或 Playwright（若已加）手动验证：错误密码出现指定错误文案；成功登录进入改密或仪表盘；退出后受保护路由不可达。</verify>
  <acceptance_criteria>
    - 源码中可检索到精确字符串 `局域网考试系统 — 学生端即将开放`
    - 源码中可检索到精确字符串 `正在验证登录状态…`
    - 源码中可检索到精确字符串 `用户名或密码错误，请检查后重试。`
    - 全仓库 `apps/web` 下 `grep -i 忘记密码` 无用户可见入口（注释除外）
    - `AdminDashboard` 或等价文件中存在 3 个 disabled 卡片且含「即将开放」
  </acceptance_criteria>
  <done>前端与 UI-SPEC 对齐，AUTH-01 用户路径可走通。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → `/api/auth/*` | 高价值凭据与 Cookie |
| 已登录教师 → `/api/admin/*` | 受保护资源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | S | Session | mitigate | `sid` 随机、`saveUninitialized: false`、登录后 `regenerate`、HttpOnly+SameSite |
| T-03-02 | T | 登录 body | mitigate | zod 校验长度上限；ORM 参数化查询 |
| T-03-03 | I | 登录响应 | mitigate | 统一错误文案；日志不记录密码 |
| T-03-04 | D | `/api/auth/login` | mitigate | `@fastify/rate-limit` + 可选延迟（文档说明） |
| T-03-05 | E | `/api/admin/*` | mitigate | `preHandler` 校验 session + `mustChangePassword` 闸门 |
</threat_model>

<verification>
- 手工或自动化：完整流程「种子账号登录 → 改密 → 仪表盘 → 退出 → 再访问受保护 API 401」。
- `GET /` 与 `/admin/login` 在局域网另一台机器可打开（依赖 01-01 网络配置）。
</verification>

<success_criteria>
ROADMAP Phase 1 成功标准 #2、#3 在验收层面可演示：**正确凭据可进管理端占位**；**错误凭据与非登录态不可访问管理功能**。
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
