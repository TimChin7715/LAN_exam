---
phase: 01-foundation-auth
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - apps/server/package.json
  - apps/server/src/index.ts
  - apps/server/src/plugins/README.md
  - apps/web/package.json
  - apps/web/vite.config.ts
  - apps/web/index.html
  - apps/web/src/main.tsx
  - apps/web/src/App.tsx
  - docker-compose.yml
  - Dockerfile
  - .dockerignore
  - README.md
  - docs/DEPLOY.md
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "docker compose up --build starts app and db without immediate crash or app alone serves GET /health with HTTP 200"
    - "GET /health returns JSON with key status and value ok"
    - "docs/DEPLOY.md documents path A bind 0.0.0.0 plus Windows firewall and path B loopback plus reverse proxy"
  artifacts:
    - pnpm-workspace.yaml
    - apps/server/src/index.ts
    - apps/web/vite.config.ts
    - docker-compose.yml
    - docs/DEPLOY.md
    - README.md
  key_links:
    - "根 `README.md` 含章节 `## 本地开发（Docker 优先）` 且给出可复制的一键命令"
---

<objective>
建立 pnpm monorepo、`apps/server`（Fastify）与 `apps/web`（Vite+React+TS）骨架；提供可运行的 `/health`；提供 Docker Compose 与 **双路径部署文档（D-06）**；为后续 Prisma 与认证计划预留目录与脚本挂钩（本计划不写业务认证逻辑）。
</objective>

<execution_context>
@E:/programs/LAN_exam/.cursor/get-shit-done/workflows/execute-plan.md
@E:/programs/LAN_exam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 monorepo 与双应用骨架</name>
  <read_first>
    - .planning/phases/01-foundation-auth/01-CONTEXT.md
    - .planning/phases/01-foundation-auth/01-RESEARCH.md
    - .planning/phases/01-foundation-auth/01-UI-SPEC.md
    - .cursor/rules
  </read_first>
  <files>package.json;pnpm-workspace.yaml;apps/server/package.json;apps/server/src/index.ts;apps/web/package.json;apps/web/vite.config.ts;apps/web/index.html;apps/web/src/main.tsx;apps/web/src/App.tsx</files>
  <action>
    创建 pnpm workspace，包名建议 `@lan-exam/server` 与 `@lan-exam/web`（可调整但须在 README 说明）。`apps/server` 使用 Fastify 5，注册 `GET /health` 返回 JSON `{ "status": "ok" }`；开发端口与 `apps/web` 的 Vite 代理对齐，使前端可通过相对路径访问 `/api`（具体端口写入 README，**默认建议** API `3001`、Web `5173` `[ASSUMED]`）。`apps/web` 使用 React + TS + Vite；按 UI-SPEC 运行 `npx shadcn@latest init`（`new-york` + `zinc` + `cssVariables: true`），并配置 Tailwind v4 与 `globals.css` 中的 zinc 语义色变量骨架。根 `package.json` 提供脚本：`dev:server`、`dev:web`、`dev`（并行）、`build`。遵守 **D-08**：`README.md` 默认推荐 Docker 开发路径。
  </action>
  <verify>在 `apps/server` 目录执行启动命令后 `curl -sSf http://127.0.0.1:<API_PORT>/health` 退出码为 0 且输出包含 `"status":"ok"`（端口以 README 为准）。</verify>
  <acceptance_criteria>
    - 仓库根存在 `pnpm-workspace.yaml` 且列出 `apps/*`
    - `apps/server/src/index.ts` 存在并注册 `GET /health`
    - `apps/web` 存在 `components.json`（shadcn init 产物）或等价配置
    - 根 `README.md` 包含 `## 本地开发（Docker 优先）` 小节
  </acceptance_criteria>
  <done>Monorepo 可安装依赖并分别启动 server/web；`/health` 可用。</done>
</task>

<task type="auto">
  <name>Task 2: Docker Compose、镜像与部署双路径文档</name>
  <read_first>
    - .planning/phases/01-foundation-auth/01-CONTEXT.md
    - README.md
  </read_first>
  <files>Dockerfile;.dockerignore;docker-compose.yml;docs/DEPLOY.md;README.md</files>
  <action>
    编写根目录 `Dockerfile`：多阶段构建——阶段一安装依赖并 `pnpm --filter @lan-exam/web build`；阶段二安装生产依赖并构建 server；最终镜像启动命令运行 `node apps/server/dist/index.js`（若先 TS 直跑，则明确为 `tsx`/`ts-node` 仅用于 dev，生产入口在文档说明）。`docker-compose.yml` 至少包含 `db`（postgres:16）与 `app`；`app` 依赖 `db` 的健康检查。**D-05/D-08**：`README.md` 首选 `docker compose up --build`。**D-06**：`docs/DEPLOY.md` 用两个二级标题分别写路径 A（监听 `0.0.0.0`、防火墙放行、局域网另一台机器访问）与路径 B（监听 `127.0.0.1`、Nginx/IIS 反代、为何在代理层 TLS）；明确本阶段 **TLS 假设** 写在文档「安全假设」段（对应 CONTEXT Planner discretion）。
  </action>
  <verify>`docker compose config` 退出码为 0；`docs/DEPLOY.md` 含字符串 `路径 A` 与 `路径 B`（或同级含义的明确小标题）。</verify>
  <acceptance_criteria>
    - `docker-compose.yml` 存在且 `db` 服务镜像为 `postgres:16`（主版本一致即可）
    - `docs/DEPLOY.md` 字数足以覆盖防火墙、反代、健康检查与回滚提示中的至少三项实操要点
    - `README.md` 引用 `docs/DEPLOY.md` 作为机房部署权威
  </acceptance_criteria>
  <done>Compose 可解析；部署文档覆盖双路径。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 机房浏览器 → 服务器 HTTP(S) | 用户可控 Host/路径/部分头 |
| CI → 构建镜像 | 供应链与密钥不进镜像层 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I | 镜像与依赖 | mitigate | 锁版本、最小基础镜像、CI `pnpm audit`（可选）并在 README 说明 |
| T-01-02 | T | 默认配置 | mitigate | 文档声明默认口令仅在 seed 阶段出现且首登改密在后续 PLAN |
| T-01-03 | D | `/health` | accept | 仅存活探测；大规模滥用由基础设施限流（文档说明） |
</threat_model>

<verification>
- `pnpm install` 成功；`pnpm -r build` 或分包 build 命令在 README 写明且可执行。
- `docker compose config` 成功。
</verification>

<success_criteria>
INFRA-01 的「可部署、可访问」骨架就绪：**Compose 可构建/解析**，**健康检查可用**，**双路径部署说明成文**。
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
