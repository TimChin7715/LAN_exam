# Phase 3 验收说明 — 名单与学生入场

**阶段：** 03-roster-student-entry  
**更新：** 2026-05-17（D-05 会话模型修订）  
**读者：** 教师验收、Phase 4 组卷与考试实现

---

## ROADMAP 成功标准（手动核对）

| # | 标准 | 验证步骤 | 通过 |
|---|------|----------|------|
| 1 | 导入名单后，管理端可按姓名或证件号检索到记录 | 教师登录 → `/admin/roster` 导入模板 → 搜索姓名或证号片段 | [ ] |
| 2 | 与名单完全一致的学生凭据可通过校验 | 使用导入的姓名+18位证号 → `/exam/login` → 进入准备页 | [ ] |
| 3 | 拒绝原因不泄露组合信息 | 故意错姓名或错证号 → 401 统一文案「姓名或身份证号不正确…」；格式错误 →「身份证号格式不正确」 | [ ] |

---

## 需求验收（ROST-01 / AUTH-02）

### ROST-01 名单导入与检索

- [ ] 下载官方模板、导入合法 xlsx 成功
- [ ] 任一行错误时整批不写入
- [ ] 列表搜索姓名或证件号有效

### AUTH-02 学生身份验证

**锁定行为（D-01～D-08）：**

| 决策 | 实现要点 |
|------|----------|
| D-01 | trim 后精确匹配 `fullName` + `nationalId` |
| D-03 | 未匹配 → 401 统一文案，无字段级提示 |
| D-04 | 格式非法 → 400「身份证号格式不正确」，不查库 |
| D-05 | 单一 `sid` Cookie；`studentRosterEntryId` 与 `teacherId` 字段隔离；verify/login 后 `saveSession`（**非** `student_sid` 双 Cookie） |
| D-06 | 准备页展示完整姓名与 18 位证号 +「请等待监考教师开始考试」 |
| D-07 | Session 仅存 `studentRosterEntryId`（及姓名快照），不存证号 |
| D-08 | 退出仅销毁学生 session；刷新准备页保持登录 |

- [ ] 同浏览器：教师已登录 `/admin` 时学生 verify 后 `GET /api/auth/me` 仍 200
- [ ] 学生退出后 `/exam/waiting` 重定向登录
- [ ] 无 session 直接访问 `/exam/waiting` → `/exam/login`

---

## 威胁假设（内网）

- 证号明文存库（D-02）：机房内网部署；HttpOnly Cookie；准备页无复制按钮（UI-SPEC）
- 枚举缓解：rate limit + 统一 401 文案

---

## Phase 4 消费说明

- 学生 session 中的 `studentRosterEntryId` 用于关联考试与提交记录
- 阅卷/组卷 MUST 在服务端校验考生仍在当次考试名单内

---

## 建议 smoke 路径

1. 教师导入 1 条测试名单
2. 学生 `/exam/login` 输入正确凭据 → 准备页见全名全号
3. 错误凭据见笼统错误；非法格式见格式错误
4. 教师端与学生端同浏览器并存会话
5. 学生退出后无法访问 waiting
