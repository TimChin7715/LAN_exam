---
phase: 03-roster-student-entry
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/server/src/plugins/session.ts
  - apps/server/src/types/session.d.ts
  - apps/server/src/lib/student-auth.ts
  - apps/server/src/lib/errors.ts
  - apps/server/src/plugins/student-guard.ts
  - apps/server/src/routes/api/student/verify.ts
  - apps/server/src/routes/api/student/me.ts
  - apps/server/src/routes/api/student/logout.ts
  - apps/server/src/index.ts
  - apps/web/src/lib/student.ts
  - apps/web/src/pages/StudentLogin.tsx
  - apps/web/src/pages/StudentWaiting.tsx
  - apps/web/src/components/auth/StudentRoute.tsx
  - apps/web/src/pages/Home.tsx
  - apps/web/src/router.tsx
  - .planning/phases/03-roster-student-entry/03-ACCEPTANCE.md
autonomous: true
requirements:
  - AUTH-02
must_haves:
  truths:
    - "Student submitting exact roster name and national ID receives student_sid session and can open waiting page"
    - "Student with wrong name or ID sees single generic error without field-level combo hints"
    - "Invalid national ID format is rejected before DB lookup with format-specific message only"
    - "Waiting page shows full name, full 18-digit ID, and fixed waiting copy per D-06"
    - "Student can logout destroying only student session; refresh waiting page stays logged in until logout or expiry"
    - "Teacher sid session unaffected when student logs in on same browser"
  artifacts:
    - path: apps/server/src/plugins/session.ts
      provides: dual cookie sid + student_sid
      contains: student_sid
    - path: apps/server/src/routes/api/student/verify.ts
      provides: roster match + session regenerate
    - path: apps/web/src/pages/StudentWaiting.tsx
      provides: D-06/D-08 waiting UI
    - path: apps/web/src/components/auth/StudentRoute.tsx
      provides: student route guard
  key_links:
    - from: apps/web/src/pages/StudentLogin.tsx
      to: POST /api/student/verify
      via: studentApi.verify
      pattern: /api/student/verify
    - from: apps/web/src/pages/StudentWaiting.tsx
      to: GET /api/student/me
      via: useEffect fetch on mount
      pattern: /api/student/me
    - from: apps/server/src/routes/api/student/verify.ts
      to: prisma.rosterEntry.findFirst
      via: exact fullName + nationalId after trim
      pattern: rosterEntry\.findFirst
---

## Phase Goal

**As a** 考生, **I want to** 用姓名与身份证号验证身份并进入考试准备页, **so that** 仅名单内且信息完全一致的考生可等待开考（AUTH-02）。

<objective>
交付 **学生入场垂直切片**：独立 `student_sid` 会话、名单强绑定校验 API、登录/准备页与路由守卫。满足 ROADMAP Phase 3 成功标准 #2–#3 及 **D-01～D-08**；依赖 03-01 的 `RosterEntry` 数据。
</objective>

<execution_context>
@E:/programs/LAN_exam/.cursor/get-shit-done/workflows/execute-plan.md
@E:/programs/LAN_exam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-roster-student-entry/03-CONTEXT.md
@.planning/phases/03-roster-student-entry/03-RESEARCH.md
@.planning/phases/03-roster-student-entry/03-UI-SPEC.md
@.planning/phases/03-roster-student-entry/03-PATTERNS.md
@apps/server/src/plugins/session.ts
@apps/server/src/routes/api/auth/login.ts
@apps/server/src/lib/roster/national-id.ts

<interfaces>
From apps/server/src/plugins/session.ts (extend, do not replace):
- Teacher: cookie name `sid`, fields SessionData.teacherId, store connect-pg-simple
- Add chained second session: name `student_sid`, save on request.raw.studentSession, restore req.session to teacher after chain (RESEARCH Pattern 3)

From apps/server/src/types/session.d.ts (extend SessionData):
- studentRosterEntryId?: string
- studentName?: string
- Do NOT add nationalId to session (D-07)

From apps/server/src/lib/errors.ts (add):
- STUDENT_AUTH_ERROR_MESSAGE = '姓名或身份证号不正确，请检查后重试。'
- INVALID_STUDENT_CREDENTIALS = 'INVALID_STUDENT_CREDENTIALS'

Student API contract:
- POST /api/student/verify body { fullName, nationalId } zod trim
- 400 VALIDATION_ERROR '身份证号格式不正确' if !isValidNationalIdFormat (D-04, no DB query)
- 401 INVALID_STUDENT_CREDENTIALS + STUDENT_AUTH_ERROR_MESSAGE if no roster match (D-03 exact ===)
- 200 { ok: true } on success after studentSession.regenerate
- GET /api/student/me → requireStudentSession → { fullName, nationalId } from RosterEntry by studentRosterEntryId
- POST /api/student/logout → destroy student session only

Web routes (03-UI-SPEC):
- / → Navigate to /exam/login or /exam/waiting if session
- /exam/login, /exam/waiting under StudentRoute
- Copy: 考生登录, 进入考场准备, 请等待监考教师开始考试
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 考生验证通过并进入准备页（全栈垂直切片）</name>
  <read_first>
    - .planning/phases/03-roster-student-entry/03-CONTEXT.md
    - .planning/phases/03-roster-student-entry/03-RESEARCH.md
    - apps/server/src/routes/api/auth/login.ts
    - apps/server/src/routes/api/auth/logout.ts
    - apps/web/src/pages/AdminLogin.tsx
  </read_first>
  <files>apps/server/src/plugins/session.ts;apps/server/src/types/session.d.ts;apps/server/src/lib/student-auth.ts;apps/server/src/lib/errors.ts;apps/server/src/plugins/student-guard.ts;apps/server/src/routes/api/student/verify.ts;apps/server/src/routes/api/student/me.ts;apps/server/src/routes/api/student/logout.ts;apps/server/src/index.ts;apps/web/src/lib/student.ts;apps/web/src/pages/StudentLogin.tsx;apps/web/src/pages/StudentWaiting.tsx;apps/web/src/pages/Home.tsx;apps/web/src/router.tsx</files>
  <action>
    **双 Session（D-05）：** 扩展 `session.ts` 为链式双 `express-session`：保留 `sid`/`teacherId` 行为；新增 `student_sid` 同 store/secret/cookie 选项（`httpOnly`, `sameSite: 'lax'`, `maxAge` 与教师一致，可用 `SESSION_MAX_AGE_MS` env 统一）。`getStudentSession(request)` 读 `request.raw.studentSession`；**禁止** 第二阶段覆盖教师 `req.session`（RESEARCH Pitfall 1）。扩展 `session.d.ts` 与 Fastify `IncomingMessage` 类型。

    **`student-auth.ts`：** `regenerateStudentSession`, `getSessionRosterEntryId`；登录成功 **仅** regenerate `studentSession`，写入 `studentRosterEntryId`, `studentName`（D-07，**不**存 nationalId）。

    **`errors.ts`：** 新增 `STUDENT_AUTH_ERROR_MESSAGE`, `INVALID_STUDENT_CREDENTIALS`（per D-08 统一失败文案，ROADMAP SC #3）。

    **`student-guard.ts`：** `requireStudentSession` — 检查 `studentRosterEntryId`，无则 401。

    **`POST /api/student/verify`（AUTH-02, D-01/D-03/D-04）：** zod body；trim；`isValidNationalIdFormat` 失败 → 400 `VALIDATION_ERROR`「身份证号格式不正确」（不查库）；`prisma.rosterEntry.findFirst({ where: { fullName, nationalId } })` 精确匹配；未找到 → 401 统一文案；成功 regenerate + set session fields；`STUDENT_VERIFY_RATE_LIMIT_MAX` 默认 20/min；日志 `event: student_verify_failed` **无** 证号。

    **`GET /api/student/me`（D-06/D-07）：** requireStudentSession；按 `studentRosterEntryId` 查 entry；返回 `{ fullName, nationalId }` 全号。

    **`POST /api/student/logout`（D-08）：** 仅 `studentSession.destroy`。

    **前端：** `lib/student.ts` — verify/me/logout（credentials include）；`StudentLogin.tsx` 镜像 AdminLogin（react-hook-form+zod）：姓名+身份证 Input（非 password，maxLength 18）；前端 `isValidNationalIdFormat` 共享逻辑（可复制 server 算法到 `apps/web/src/lib/national-id.ts` 或轻量 duplicate）；格式错不请求；401 显示 STUDENT_AUTH_ERROR_MESSAGE；成功 `navigate('/exam/waiting', { replace: true })`；文案「考生登录」「进入考场准备」（UI-SPEC S6）。

    **`StudentWaiting.tsx`（D-06）：** mount 调 `GET /api/student/me`；展示完整姓名+完整证号（font-mono）；固定句「请等待监考教师开始考试」；outline「退出」→ logout → `/exam/login`。

    **路由：** `Home.tsx` → session 感知 redirect（已登录学生 → waiting，否则 login）；`router.tsx` 增加 `/exam/login`, `/exam/waiting`（本任务可先无守卫，Task 2 补全）；**不** 包裹 AdminRoute。

    注册 student 路由于 `index.ts`。
  </action>
  <verify>
    <automated>pnpm --filter @lan-exam/server exec tsc -p tsconfig.json --noEmit</automated>
    <automated>pnpm --filter @lan-exam/web exec tsc -b --noEmit</automated>
    <automated>rg -q "student_sid" apps/server/src/plugins/session.ts; rg -q "STUDENT_AUTH_ERROR_MESSAGE" apps/server/src/lib/errors.ts; rg -q "student/verify" apps/server/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - 名单内精确匹配考生 POST verify 200 且 Set-Cookie 含 student_sid；GET /api/student/me 返回全号
    - 错误组合 401 且 message 为统一文案（响应无 field 暗示）
    - 格式非法 400「身份证号格式不正确」且未增加 DB 查询（可测不存在证号格式错）
    - 教师已登录 sid 时学生 verify 后 `GET /api/auth/me` 仍 200
    - 浏览器：登录 → 准备页见姓名+证号+等待文案 → 退出回登录
  </acceptance_criteria>
  <done>AUTH-02 主路径可用；双 session 不互踢。</done>
</task>

<task type="auto">
  <name>Task 2: 学生路由守卫、会话边界与阶段验收文档</name>
  <read_first>
    - .planning/phases/03-roster-student-entry/03-UI-SPEC.md S8
    - apps/web/src/components/auth/AdminRoute.tsx
    - .planning/ROADMAP.md Phase 3 success criteria
  </read_first>
  <files>apps/web/src/components/auth/StudentRoute.tsx;apps/web/src/router.tsx;.planning/phases/03-roster-student-entry/03-ACCEPTANCE.md</files>
  <action>
    **`StudentRoute.tsx`（UI-SPEC S8）：** 检查学生 session（`studentApi.me` 或轻量 context）；`checking` → Spinner「正在验证考生身份…」；未登录访问 `/exam/waiting` → redirect `/exam/login`；已登录访问 `/exam/login` → redirect `/exam/waiting`；401 时 toast「登录已过期，请重新验证身份。」。与 AdminRoute **隔离**。

    **`router.tsx`：** `/exam` 嵌套 `StudentRoute`；`waiting` 路由置于需学生 session 的子布局；`/` redirect 逻辑与 Task 1 对齐。

    **`03-ACCEPTANCE.md`：** 记录 Phase 3 三条 ROADMAP 成功标准的手动验证步骤；锁定 AUTH-02 行为（trim-only、格式门闸、统一 401、session 不存证号、准备页全号）；注明 Phase 4 消费 `rosterEntryId`；内网明文存储威胁假设（D-02）引用 plan threat_model。

    **加固：** 确认 verify/me/logout **不** 使用 `requireAdminSession`；准备页无教师/admin 链接；学生 API 错误响应不含 `nationalId`/`fullName` 组合提示字段。
  </action>
  <verify>
    <automated>rg -q "StudentRoute" apps/web/src/router.tsx; rg -q "." .planning/phases/03-roster-student-entry/03-ACCEPTANCE.md</automated>
    <automated>rg -q "成功标准" .planning/phases/03-roster-student-entry/03-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - 无 session 直接访问 `/exam/waiting` 被重定向登录
    - 有 session 访问 `/exam/login` 重定向 waiting
    - `03-ACCEPTANCE.md` 含三条 ROADMAP SC 可勾选项与 AUTH-02/D-01～D-08 摘要
  </acceptance_criteria>
  <done>学生路由边界清晰；Phase 3 验收文档就绪。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 学生浏览器 → POST /api/student/verify | 不可信姓名/证号；服务端权威校验 |
| 学生 session → GET /api/student/me | 仅可读本人 rosterEntryId 对应记录 |
| 学生 vs 教师 Cookie | 独立 `student_sid` / `sid`（D-05） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | I | 名单枚举（试探组合） | mitigate | 401 统一文案；rate limit；D-04 格式门闸减少查库 |
| T-03-08 | S | 会话固定 | mitigate | `studentSession.regenerate` on verify success |
| T-03-09 | I | 证号写入 session/日志 | mitigate | D-07 session 仅存 rosterEntryId；warn 日志无全号 |
| T-03-10 | D | verify 暴力请求 | mitigate | STUDENT_VERIFY_RATE_LIMIT_MAX per-route |
| T-03-11 | E | 越权读他人 /me | mitigate | requireStudentSession + lookup by session id only |
| T-03-12 | I | 双 session 覆盖导致教师掉线 | mitigate | 链式中间件恢复 teacher session；验收同事务 cookie |
| T-03-13 | I | XSS 窃取证号 | accept | 内网机房；HttpOnly cookie；准备页无复制按钮（UI-SPEC） |
</threat_model>

<verification>
- 导入测试名单后：正确凭据 → 准备页全名全号；错误姓名 → 401 笼统文案。
- 同浏览器：教师 `/admin` 仍可用同时学生已登录 waiting。
- 退出后 waiting 不可访问；刷新 waiting 保持登录（D-08）。
</verification>

<success_criteria>
AUTH-02 满足；ROADMAP Phase 3 成功标准 #2–#3 可测；`rosterEntryId` 在 session 中可供 Phase 4 关联考试。
</success_criteria>

<output>
完成后创建 `.planning/phases/03-roster-student-entry/03-02-SUMMARY.md`
</output>
