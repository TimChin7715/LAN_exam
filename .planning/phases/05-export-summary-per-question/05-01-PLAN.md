---
phase: 05-export-summary-per-question
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/export/test1-成绩导出.xlsx
  - apps/server/src/lib/exam/export-summary.ts
  - apps/server/src/lib/exam/export-workbook.ts
  - apps/server/src/lib/exam/export-workbook.test.ts
autonomous: false
requirements:
  - EXPR-03
  - EXPR-04
must_haves:
  truths:
    - "Teacher opens exported xlsx 成绩汇总 and sees fixed 5 columns then 第1题…第N题 for N = exam question count"
    - "Submitted students have per-question cells matching 答题明细 得分 for same name and 题号"
    - "Unsubmitted students show — for total and every question column with 未提交"
    - "Submitted student missing Answer row for a question shows 0 in that question column"
    - "答题明细 sheet still has 8 columns and only submitted students rows unchanged from v1.0"
  artifacts:
    - path: fixtures/export/test1-成绩导出.xlsx
      provides: school reference fixture per D-07
    - path: apps/server/src/lib/exam/export-summary.ts
      provides: pure pivot helpers for summary question cells
      exports: perQuestionScoresForSummary, buildSummaryRowQuestionFields
    - path: apps/server/src/lib/exam/export-workbook.ts
      provides: extended buildExamExportWorkbook with dynamic summary columns
      exports: buildExamExportWorkbook
    - path: apps/server/src/lib/exam/export-workbook.test.ts
      provides: D-08 automated contracts and fixture header smoke
  key_links:
    - from: apps/server/src/lib/exam/export-workbook.ts
      to: prisma.examQuestion.findMany
      via: orderBy sortOrder asc before summary columns
      pattern: examQuestion\.findMany
    - from: apps/server/src/lib/exam/export-workbook.ts
      to: apps/server/src/lib/exam/export-summary.ts
      via: per-question row fields from pointsAwarded map
      pattern: perQuestionScoresForSummary|buildSummaryRowQuestionFields
    - from: apps/server/src/lib/exam/export-workbook.test.ts
      to: fixtures/export/test1-成绩导出.xlsx
      via: exceljs xlsx.load header smoke
      pattern: fixtures/export/test1
---

<objective>
Extend `buildExamExportWorkbook` so **成绩汇总** is a wide table (fixed 5 columns + 第1题…第N题), keep **答题明细** unchanged, commit school fixture, and lock behavior with automated tests (D-07, D-08).

Purpose: Deliver EXPR-03/04 for milestone v1.1 — per-question scores visible in summary for external system import.
Output: `export-summary.ts`, updated `export-workbook.ts`, `export-workbook.test.ts`, `fixtures/export/test1-成绩导出.xlsx`.
</objective>

<execution_context>
@E:/programs/LAN_exam/.cursor/get-shit-done/workflows/execute-plan.md
@E:/programs/LAN_exam/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-export-summary-per-question/05-CONTEXT.md
@.planning/phases/05-export-summary-per-question/05-RESEARCH.md
@.planning/phases/05-export-summary-per-question/05-PATTERNS.md
@.planning/phases/04-exam-submit-export/04-ACCEPTANCE.md
@.planning/codebase/TESTING.md
@apps/server/src/lib/exam/export-workbook.ts
@apps/server/src/lib/exam/score-question.test.ts

<interfaces>
From `export-workbook.ts` (Phase 4 baseline — detail sheet L106-139 unchanged):
- `buildExamExportWorkbook(examId: string): Promise<ExcelJS.Workbook>`
- Summary fixed columns: 姓名, 身份证号 (maskNationalId), 总分, 是否提交, 提交时间
- Detail: 8 columns; only submitted; `num: sortOrder + 1`, `points: pointsAwarded`

From `submit.ts` (authoritative question order):
- `examQuestion.findMany({ where: { examId }, orderBy: { sortOrder: 'asc' } })`
- Every submitted exam creates one Answer per ExamQuestion with `pointsAwarded` (0 if wrong/empty)

From `mask-national-id.ts`:
- `maskNationalId(id: string): string` — 18-digit → first6 + ******** + last4; else '—'

New contracts (`export-summary.ts`):
- `perQuestionScoresForSummary(examQuestions, submission?) → (number | '—')[]`
- `buildSummaryRowQuestionFields(examQuestions, submission?) → Record<string, number | '—'>` with keys `q_${examQuestion.id}`

Prisma change in export query: add `examQuestion.id` to answers select (pivot key); do NOT call scoreQuestion at export time.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Commit school export fixture (D-07)</name>
  <read_first>
    - .planning/phases/05-export-summary-per-question/05-CONTEXT.md (D-07, canonical_refs)
    - .planning/phases/05-export-summary-per-question/05-RESEARCH.md (Fixture pitfall 4)
    - .planning/phases/05-export-summary-per-question/05-PATTERNS.md (fixture analog, verify-parse pattern)
  </read_first>
  <files>fixtures/export/test1-成绩导出.xlsx</files>
  <action>
    Per **D-07**, add校方样例 at repo root `fixtures/export/test1-成绩导出.xlsx` (create `fixtures/export/` if missing).

    **If file already exists in workspace:** Open「成绩汇总」sheet; confirm column 1–5 are 姓名、身份证号、总分、是否提交、提交时间; column 6+ are 第1题、第2题… (contiguous). Confirm no full 18-digit national IDs in any cell (must match `maskNationalId` pattern or synthetic). Commit the binary fixture.

    **If file missing:** Obtain `test1-成绩导出.xlsx` from the school/operator (same file referenced in Phase 5 discuss). Place at exact path above. Redact any plaintext 18-digit IDs before commit. Do not commit real student PII.

    Do **not** add freeze panes, column styling, or extra sheets beyond what the sample contains.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path 'fixtures/export/test1-成绩导出.xlsx')) { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `fixtures/export/test1-成绩导出.xlsx` exists in git
    - Worksheet「成绩汇总」present with 第1题 as column 6 header (or documented mismatch noted in SUMMARY if sample differs — implementation still follows D-01)
    - No full 18-digit national ID strings in fixture cells
  </acceptance_criteria>
  <done>Fixture on disk and committed; D-07 reference file available for smoke tests.</done>
</task>

<task type="auto">
  <name>Task 2: Pure pivot + wide 成绩汇总 sheet (D-01–D-06, EXPR-03)</name>
  <read_first>
    - apps/server/src/lib/exam/export-workbook.ts
    - apps/server/src/lib/exam/submit.ts (examQuestion.findMany pattern)
    - .planning/phases/05-export-summary-per-question/05-PATTERNS.md
    - .planning/phases/05-export-summary-per-question/05-RESEARCH.md (Pattern 1–3)
  </read_first>
  <files>apps/server/src/lib/exam/export-summary.ts;apps/server/src/lib/exam/export-workbook.ts</files>
  <action>
    Create **`export-summary.ts`** with testable pure functions (per RESEARCH Pattern 4):

    1. `perQuestionScoresForSummary(examQuestions: { id: string; sortOrder: number }[], submission?: { answers: { examQuestion: { id: string }; pointsAwarded: number }[] })` — if no submission return all `'—'` (**D-03**); else map `pointsAwarded` by `examQuestion.id`, missing id → `0` (**D-04**), preserve numeric `0` (**D-05**).

    2. `buildSummaryRowQuestionFields(examQuestions, submission?)` — returns `{ [q_${id}]: number | '—' }` using stable keys `q_${examQuestion.id}` (NOT sortOrder).

    Modify **`export-workbook.ts`** only for summary sheet + data loading:

    - After `exam` load, `await prisma.examQuestion.findMany({ where: { examId }, orderBy: { sortOrder: 'asc' }, select: { id: true, sortOrder: true } })` — N and header order from this alone (**D-01**, RESEARCH Pattern 1).

    - Extend roster `answers.examQuestion.select` to include `id: true` (keep existing fields for detail loop).

    - Replace static 5-column `summarySheet.columns` with fixed five + `examQuestions.map(eq => ({ header: \`第${eq.sortOrder + 1}题\`, key: \`q_${eq.id}\`, width: 10 }))` (**D-01**). Keep `getRow(1).font = { bold: true }`. **Do not** add freeze panes or custom widths beyond width: 10 on question cols (**D-06**).

    - For each roster row: build fixed fields as today; spread `buildSummaryRowQuestionFields(examQuestions, submission)` into `addRow`. Submitted: `score` = `submission.totalScore` (number); unsubmitted: `score` = `'—'`, all question keys `'—'` (**D-03**). Question values only from `pointsAwarded` (**D-02**), never recompute via `scoreQuestion`.

    - **Leave detail sheet block (L106–139) byte-for-byte behavior unchanged** — same columns, same loop, same `continue` for unsubmitted (**EXPR-04**).

    - N=0: only five fixed columns, no dynamic headers (discretion).

    - Keep scale comment; no new `console.log` with `nationalId`.
  </action>
  <verify>
    <automated>pnpm --filter @lan-exam/server exec tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `export-summary.ts` exports pure pivot helpers; no Prisma/ExcelJS imports
    - Summary headers: 姓名…提交时间 then 第1题…第N题 where N = examQuestions.length
    - Unsubmitted row: total and every `q_*` cell is em-dash `—`
    - Submitted row: `q_*` uses pointsAwarded; missing answer → 0; zero score → number 0
    - Detail sheet code path unchanged (8 headers, submitted-only rows)
  </acceptance_criteria>
  <done>EXPR-03 wide summary implemented; EXPR-04 detail preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: export-workbook tests + fixture smoke (D-08)</name>
  <read_first>
    - apps/server/src/lib/exam/score-question.test.ts
    - apps/server/src/lib/exam/export-summary.ts
    - .planning/codebase/TESTING.md
    - .planning/phases/05-export-summary-per-question/05-RESEARCH.md (Consistency helper, Fixture smoke)
  </read_first>
  <files>apps/server/src/lib/exam/export-workbook.test.ts</files>
  <behavior>
    - perQuestionScoresForSummary: unsubmitted → all '—'
    - perQuestionScoresForSummary: submitted with answers [2,0] → same numbers; missing answer id → 0
    - perQuestionScoresForSummary: explicit zero → 0 not empty
    - buildSummaryRowQuestionFields: keys q_eq1, q_eq2 match examQuestions ids
    - In-memory workbook: writeBuffer → load → row1 col6 === '第1题' for 2-question exam
    - assertSummaryDetailConsistency helper: for synthetic submitted student, summary 第k题 === detail 得分 where 题号=k and 姓名 match
    - Integration: at least one test calls `buildSummaryRowQuestionFields` + production summary column builder path used by `export-workbook.ts` (extract `buildSummarySheetColumns(examQuestions)` if needed so tests exercise same header/key contract as production)
    - Detail regression: worksheet 答题明细 has exactly 8 header cells (姓名…得分)
    - Fixture smoke (if file exists): load fixtures/export/test1-成绩导出.xlsx; 成绩汇总 row1 includes 姓名 and 第1题; skip with message if missing (should not skip after Task 1)
  </behavior>
  <action>
    Add **`export-workbook.test.ts`** using `node:test` + `node:assert/strict` + `tsx` (no Prisma mocks).

    **Unit tests** for `export-summary.ts`: cover D-03, D-04, D-05 cases in behavior block.

    **ExcelJS round-trip test:** Use the same summary column builder exported from `export-workbook.ts` (or shared module imported by both production and tests) — build minimal Workbook, add one submitted + one unsubmitted row, `writeBuffer` → `load`, assert headers and cell values match production contract.

    **Production wiring test:** Assert `export-workbook.ts` imports `buildSummaryRowQuestionFields` from `export-summary.ts` and calls `examQuestion.findMany` before setting `summarySheet.columns` (structural test via re-exported `buildSummarySheetColumns` or snapshot of header keys from shared helper).

    **Summary ↔ detail consistency (D-08):** Implement `assertSummaryDetailConsistency(workbook, studentName)` that reads「成绩汇总」and「答题明细」: for each k in 1..N, compare summary cell under header `第${k}题` to detail row with `题号===k` and same `姓名`, column `得分`. Use at least one submitted fixture with 2+ questions and mismatched scores to prove detection would fail.

    **EXPR-04 regression:** Assert detail sheet name and 8 column headers unchanged from Phase 4 list.

    **Fixture smoke (D-07):** Resolve path from repo root (`import.meta.url` → `fixtures/export/test1-成绩导出.xlsx`). `readFileSync` + `workbook.xlsx.load`; assert 成绩汇总 exists; `getRow(1)` values include 姓名, 第1题 (and 第2题 if N≥2 in sample). Use `existsSync` guard only for local dev without fixture — CI must have file after Task 1.

    Run: `node --import tsx --test apps/server/src/lib/exam/export-workbook.test.ts`
  </action>
  <verify>
    <automated>node --import tsx --test apps/server/src/lib/exam/export-workbook.test.ts</automated>
    <automated>pnpm --filter @lan-exam/server exec tsc -b --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass locally
    - At least one test asserts unsubmitted question columns are '—'
    - At least one test asserts summary 第k题 equals detail 得分 for same student/question
    - At least one test asserts detail sheet still has 8 columns
    - Fixture header test runs when xlsx present (post Task 1)
  </acceptance_criteria>
  <done>D-08 automated coverage in place; D-07 fixture validated by header smoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin session → export API | Only authenticated teacher; exam.teacherId must match (unchanged Phase 4) |
| Export workbook → file download | xlsx may contain masked IDs and scores; no new endpoints |
| Fixture commit → repository | Binary must not contain plaintext 18-digit national IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information disclosure | export-workbook summary columns | mitigate | Continue maskNationalId on 身份证号; never log nationalId |
| T-05-02 | Information disclosure | fixtures/export/test1-成绩导出.xlsx | mitigate | Human checkpoint: verify no full IDs before commit; header-only CI assertions |
| T-05-03 | Denial of service | buildExamExportWorkbook wide table | accept | Same ~2000×200 scale note; extra N columns linear memory — acceptable per 04-SECURITY R-04-03-01 |
| T-05-04 | Tampering | Client-side xlsx | accept | Export is read-only artifact; authoritative data remains DB |
</threat_model>

<verification>
- `node --import tsx --test apps/server/src/lib/exam/export-workbook.test.ts` exits 0
- `pnpm --filter @lan-exam/server exec tsc -b --noEmit` exits 0
- Manual (post-execute): export ended exam → open xlsx → 成绩汇总 has 5+N columns; 答题明细 unchanged
</verification>

<success_criteria>
- EXPR-03: Wide summary with per-question scores per CONTEXT D-01–D-05
- EXPR-04: Detail sheet behavior and columns match v1.0
- D-07: Fixture committed at fixtures/export/test1-成绩导出.xlsx
- D-08: Automated summary/detail consistency and edge-case tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-export-summary-per-question/05-01-SUMMARY.md`
</output>
